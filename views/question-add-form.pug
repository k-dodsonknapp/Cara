extends layout.pug

include utils.pug

block content
  +validationErrorSummary(errors)
  form(action='/question/add' method='post')
    include park-add-form.pug
    div(class='py-4')
      button(type='submit' class='btn btn-primary') Add Question
      a(href='/' class='btn btn-warning ml-2') Cancel
