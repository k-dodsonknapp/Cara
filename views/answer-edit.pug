extends question-detail.pug

include utils.pug

block content
    +validationErrorSummary(errors)
    form(action="/answer/:id(\\d+)/edit" method='post')
        include answer-form.pug
        button(type='submit') Edit Answer
        a(href='/questions/:id(\\d+)/answers') Cancel
